# Runner and Model Configuration (WS-H)

This content moved to [../agents.md](../agents.md), the canonical reference for the crew roster,
runner and model configuration, the provider registry and cost classes, and worked recipes
(flipping a role to a local runner, routing spend to a local or free model endpoint, a fully
local zero-cost maker/checker pair, and the distinct-model-family enforcement rule).
